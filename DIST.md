# voicectl の配布手順（フェーズ A4・2026-09-22 時点の確定方針）

**配布形態は「同梱フォルダ方式」に決定**（PyInstaller の単一 exe は不採用）。
理由: 音声認識（faster-whisper + CUDA）と PySide6 を 1 実行ファイルに固めると数百 MB になり、
起動時の展開も遅い。ソースフォルダ + run.bat のセルフセットアップなら、中身が見えて
トラブル時に差し替えられる。**開発は続く**ので、zip を作り直すだけで更新できる形が合っている。

## 配布 zip の作り方（配布する人）

1. 作業フォルダに下の内容だけを入れる:
   - `voicectl/`（コード一式）、`profiles/`、`prompts/`、`tests/` は省略可
   - `run.bat`、`requirements.txt`、`config.yaml`、`README.md`、`DESIGN.md`
   - **含めない**: `.venv/`、`logs/`、`state/`、`cache/`、`app_readings.yaml`、`.mimosa/`、`.git/`
     （個人の状態と秘密情報。.gitignore と同じ一覧）
2. フォルダを zip にする（voicectl.zip など）
3. 受け取り側には次を伝える: Python 3.12 のインストール、zip の展開、run.bat のダブルクリック。
   初回は仮想環境の作成で数分かかること、起動後に初回セットアップの画面が出ること

## 受け取り側の初回起動の流れ

1. `run.bat` をダブルクリック → 仮想環境の作成と依存のインストール（数分・自動）
2. トレイにマイクのオーブが出る。API キーが無いと **初回セットアップのウィザードが自動で開く**:
   ようこそ → マイク選択とテスト → 話しかけキー → API キー（環境変数に登録）→ GPU の確認 → 完了
3. 「保存して再起動」で設定が反映される。あとは README の言い方で操作できる

## 既知の制約（配布時に伝えること）

- **GPU**: 音声認識は NVIDIA GPU（設定の `stt.gpu_name` に一致するもの）で速く動く。
  GPU が無い PC では `stt.device: auto` により自動で CPU に落ちる（応答が数秒かかることがある）
- **マイク**: リモートデスクトップ接続中は PC 本体のマイクが使えないことがある（README の「ブラウザをマイクにする」）
- **API キー**: Jev 用の TYPESAFE_API_KEY が必要（設定ウィンドウの「連携」からも登録できる）

## 今後（v0.2 以降の検討）

- 更新の配布方法（zip 差し替えの手順書、または git pull の案内）
- PyInstaller 単一 exe の再検討（需要が出てから）
- インストーラー（Inno Setup 等）と自動起動（スタートアップ登録）の UI
