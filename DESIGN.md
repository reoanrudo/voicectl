# voicectl 設計書（要件定義と今後の開発）

2026-09-22 時点。開発は organically に広がったため、ここで一度「目的・原則・スコープ」を固定し、
今後の変更はこの文書に照らして判断する。変更が合意されたらこの文書を更新する（コードだけ変えない）。

## 1. このアプリの目的

**「専用アプリがバラバラに行っていた PC 操作を、話し言葉という一つの文法に統合する」**

- クロック（アラーム）・音量ミキサー・エクスプローラー・窓配置ツール・圧縮ソフト・スクショツール・
  マクロツール——それぞれ専用アプリを開くかショートカットを暗記する必要があったものを「〇〇して」で済ませる
- しかも使うほど**自分の言い方を覚える**（学習・ルーチン）。単なる音声リモコンではなく、自分に育つ統合インターフェース
- 判定の主役は **Jev**（約 200〜300ms で型の決まった選択肢から選び、確率と確信度を返す）。
  LLM エージェント的な重い作りにはしない

## 2. 対象ユーザーと利用場面

| 項目 | 内容 |
|---|---|
| 第一のユーザー | 作業しながら手が離せない人。実際の運用: 開発（ZCode）・JWCAD・GTO ツール・ブラウザを行き来する作業 |
| 配布対象 | Windows 11 + NVIDIA GPU（音声認識を GPU で行う）がある power user |
| 利用場面 | 両手が塞がっている / ショートカットを暗記したくない / 反復する手順を一言にまとめたい |
| 入力方法 | 基本は F5 押しながら話す。常時聞き取り（F5 タップ）は補助。待機中の常時待機は今はしない |

## 3. 設計原則（すべての機能追加はこの 7 か所に照らす）

1. **速さ**: 確定した命令は約 0.35 秒、Jev 経路は約 0.6 秒で動く。これを悪化させる機能は入れない
2. **シェルは使わない**: ターミナル実行のレーンは作らない（2026-09-22 確定意向）。
   PC 操作は Win32・UIA・Shell COM・Python 標準ライブラリの範囲で行う
3. **危険なことは確認する**: 低確信度・取消しにくい操作は「はい/いいえ」確認を通す。
   低確信度のまま実行しない（0.05 でクリック、は起きない。確認待ちに落ちる）
4. **文法は文書化する**: 声でできることは README に載る。暗黙の機能を積まない
5. **設定は UI から**: 設定変更は設定ウィンドウ（config.yaml を読み書き）の領分。声では設定を変えさせない
6. **個人的状態は配布しない**: 学習・ルーチン・メモ・ログは state/logs/cache に置き、配布物に含めない
7. **テストで守る**: 解析（route）だけでなく実行経路のテストも残す。tests/test_accuracy.py のマトリクスに
   言い方を登録してから、壊れない

## 4. 現在の機能の棚卸し（2026-09-22 時点）

- **判定の段階**: 停止/確認/ヒント → 決まった形の命令（intents）→ 目的ひな形（tasks）→ 学習済み言い方 →
  分割実行 → キーワード即実行 → 画面を見て keyword+Jev → LLM 言い直し → 多段エージェント
- **決まった形の命令**: 音量（全体/アプリ別）・タブ番号・動画シーク・ページ内検索・ファイル検索・
  窓の並べ方（左右/上下/ひな形 layouts）・名前つき窓操作・時刻/計算・タイマー・アラーム（標準クロック）・
  読み上げ・書き取り・選択して検索・行編集・音声メモ・最新ダウンロード・Cドライブ・zip/展開/チェックサム
- **キー即実行**: コピー系・メディア・スクショ・ズーム・ブラウザ操作・窓スナップ（半分/四分の一/中央）等
- **エクスプローラー**: 並べ替え・n 番目を開く・コピー/移動・パスコピー・ターミナルを開く・選択して開く
- **学習**: 言い方を覚え、3 回成功で確認なしに昇格。ルーチン（声で登録）とやって見せた記録
- **設定ウィンドウ**: マイク/キー・判定しきい値・layouts・学習とルーチンの管理・API キー
- **周辺**: ブラウザマイク（リモート接続時）、動作確認用の窓口（127.0.0.1:8766/say）

## 5. やらないこと（非スコープ。提案が来たらこの行に戻って判断する）

- ターミナル/シェル実行のレーン（確定意向。Win32/UIA で同等の速さを実現する方向で考える）
- 読み上げ返答（不要の確定意向。`voice_reply.enabled: false` のまま。再開を提案しない）
- ウェイクワード常時待機（F5 反応悪化のため無効化済み。再挑戦には「反応を悪化させない設計」の要件が先）
- チャットボット化・汎用対話アシスタント化（命令の実行に特化。雑談は捨てる）
- 管理者権限を要求する操作の自動化（UAC が出るものは扱わない）
- macOS / Linux / モバイル対応
- 声のテキストをそのままコマンドに埋め込む系の機能（インジェクション禁止）
- **ランチャー連携（Raycast など）**（2026-09-23 確定意向。実験の結果: 再起動で表示/非表示がトグルする・
  UI が自己描画で候補を読めない・日本語のアプリ名が索引に乗らない、と確実性が不足。
  入力先の前面確認を徹底しても誤入力リスクが残り、得られる便利さが割に合わない）

## 6. 今後の開発（優先順位）

### フェーズ A: 配布の完成（2026-09-22 実装済み。受け取りテストが残る）
| # | 項目 | 内容 | 完了の定義 | 状態 |
|---|---|---|---|---|
| A1 | 初回ウィザード | マイク確認→キー選択→API キー→GPU 確認→完了 | 初見の PC で案内どおりに動かせる | 実装済み・実地未確認 |
| A2 | 設定の移行 | config.yaml の version を見た移行処理 | 旧設定で起動して新形式に更新される | 実装済み（テストあり） |
| A3 | CPU フォールバック | `stt.device: auto` で GPU が無ければ CPU へ | CPU のみの PC で動作報告 | 実装済み・実測は今後 |
| A4 | 配布形態の確定 | 同梱フォルダ方式（run.bat がセルフセットアップ）。手順は DIST.md | 第三者に渡して動かせた記録 | 方針確定・実地は今後 |
| A5 | 全体セキュリティ監査 | Mimosa deep スキャンを 1 回走らせ、指摘を潰す | 監査レポート完遂 | 実行中 |

### フェーズ B: 日常使いの精度（実地からの改善ループ）
| # | 項目 | 内容 | 状態 |
|---|---|---|---|
| B1 | 実地確認の週次ループ | 声で試す → decisions ログ分析 → 修正 → マトリクス追加（既存の型を続ける） | 継続（声の試用はユーザーと） |
| B2 | 「ありません」応答 | 存在しないメニューを頼まれたときに黙って失敗しない | 実装済み |
| B3 | 状況別の語義 | ターミナルでのコピー/貼り付けを Shift 付きに（仕組みは facts.window_kind） | 実装済み（種は最小） |
| B4 | タスクバー・通知領域 | 「タスクバーの3番目」「タスクバーのBrave」 | 実装済み（通知領域は未） |
| B5 | 話し中の予測バー | 話している最中に「→ 保存をクリック」の予想を表示 | 実装済み |

### フェーズ C: 統合の深掘り（特化アプリ置き換えの拡大）— 2026-09-22 実装済み
C1 後で読む（ブラウザの URL を UIA で読み state/read_later.txt へ。一覧も）/
C2 会議ミュート（profiles/teams.yaml・zoom.yaml。実地未確認）/
C3 重いプロセスを番号で止める（確認あり。名前が変わったら中止）/
C4 画面の拡大（拡大鏡。Win+Plus/Minus）

## 7. 完了の定義（すべての機能に共通）

1. README に言い方（文法）が載っている
2. tests/test_accuracy.py のマトリクスに登録してある（正例と、奪わないことを示す反例）
3. 全テスト + smoke_commands 合格
4. 実地確認 1 回以上（危険度ゼロの表示系は「省略」と明記してよい）
5. HANDOFF に実装メモ（設計判断の記録）

## 8. 開発ループの型（ふだんの回し方）

```
設計書のフェーズから 1 件選ぶ
  → 実装 → テスト/マトリクス追加 → smoke → （実地） → ログ分析
  → README/HANDOFF 更新 → コミット・プッシュ
新しいアイデアが浮かんだら: 実装せず、まずこの文書のフェーズ表に追記して優先度を付ける
```

## 9. 目標値（体感の約束）

- 初動（確定命令）: F5 を離してから **0.35 秒以内**に動き始める
- Jev 経路: **0.6 秒以内**（先読みの効果を維持）
- マトリクス: 常に **100% 通過**（落ちたら機能追加より先に直す）
- 起動: **10 秒以内**にトレイが出る
