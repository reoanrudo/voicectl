# 引き継ぎ：voicectl（Jev を使った Windows 音声操作アプリ）

あなたは S:\voice-control の音声操作アプリ「voicectl」の開発を引き継ぎます。
まずこのファイルと README.md、**DESIGN.md（設計書・要件定義。目的・原則・非スコープ・フェーズ表）**を読み、
状況を確認してから作業してください。新しい機能の提案は実装せず、まず DESIGN.md のフェーズ表に追記して
優先度を付けます。

## 守ること（最重要）

- **言語**：回答・コメント・コミットメッセージはすべて日本語にする。中国語・英語の文章は使わない。
- **方向性**：ユーザーが求めているのは「LLM エージェント」ではなく、**Jev の良さを活かした直感的な操作と、Windows の理解**。
  - Jev の良さ：約 200〜300 ms で、型の決まった選択肢から選び、確率と確信度を返す。
  - 「コーディングエージェント向け」のような重い作りにはしない。
  - DeepSeek は、文章の生成と、Jev で決まらなかったときの補助に限る。
- **GPU**：CUDA は RTX 4080 SUPER だけで使う。RTX 3060 は PCIe の BusReset から BSOD の起点になったので使わない。
- **デバイス**：デバイスを総当たりで開くような調査はしない（開いた直後にバグチェック 0x116 で再起動した）。
- **実地テストでのキー入力**：こちらのツールを動かすと前面が ZCode や Claude に移る。
  - 過去に、テストの文字をユーザーの ZCode に誤って入力した。
  - キー入力を伴う実地テストは原則しない。するときは必ず前面を確かめる。
  - 操作を伴うテストの前には、ユーザーの了承を得る。直近では、テスト用メッセージボックスを出すテストを断られた。
- **システム設定**：システムやセキュリティの設定は変えない。
- **秘密情報**：API キー（環境変数 TYPESAFE_API_KEY）は、ファイルにもログにも書かない。

## 構成（主なファイル）

| ファイル | 役割 |
|---|---|
| voicectl/controller.py | 中心。_handle_text の処理順は下の「処理の順番」を参照 |
| voicectl/intents.py | 決まった形の命令（音量・タブ・ファイル・配置・タイマー・アラーム・設定画面・電源・書き取り・ルーチン・記録・AI・エクスプローラー・連続移動）。Jev を通さず即実行 |
| voicectl/tasks.py | サイトを開く・検索（「X内で〇〇を検索」「ここで〇〇を検索」も含む）・未知サイト（DuckDuckGo の「\」検索） |
| voicectl/winctx.py（新） | Windows の状況：窓の種類・ダイアログとそのボタン・フォーカス・エクスプローラーのフォルダと選択。Shell COM での並べ替え・n 番目を開く・コピー/移動・パスのコピー・ターミナル |
| voicectl/engines/jev.py | Jev への状態と質問の組み立て。facts.windows の説明を追加した。候補の上位確率を Decision.alternatives に入れる |
| voicectl/engines/llm.py | DeepSeek（opencode 経由）の判定補助。say で「アプリが実行できる言い方」へ言い直す |
| voicectl/assistant.py | 文章アシスタント（書く・要約・翻訳・書き直し・質問・画面・振り返り）。プロンプトは .opencode/agents/voicectl-writer.md |
| voicectl/demo.py | やって見せた操作の記録と再生、エージェントの成功手順の再生 |
| voicectl/overlay.py | オーロラガラスの UI（状態カプセル・文字起こしバー・答えカード AnswerCard・番号ピル） |
| voicectl/debug_inbox.py | 動作確認用の窓口。127.0.0.1:8766 で `POST /say`（本文＝命令文、UTF-8）と `GET /status` を受け付ける |
| config.yaml | 下の「設定の現状」を参照 |

### 処理の順番（controller._handle_text）

1. 停止
2. 連続移動の停止
3. 電源・移動の確認
4. 下書きへの返事
5. 書き取り
6. 確認待ち
7. ヒント・候補の番号
8. 続き
9. 言い直し
10. ダイアログへの返事
11. intents
12. 回数つきの命令
13. tasks
14. 学習した言い方
15. 分割
16. Jev
17. 候補の番号の提示
18. LLM
19. エージェント

### 設定の現状（config.yaml）

- `decision.llm_fallback`：backend が opencode、モデルが deepseek/deepseek-v4-flash で有効。
- `assistant.enabled`：true。
- `debug.inbox`：true。
- `overlay.font_px`：22。`transcript.font_px`：18。
- `routines`：「作業開始」がある。
- `files.extra_roots`：S:\。

## 直近の作業（2026-09-22 夜）と状態

ユーザーの依頼：「直感的操作を強めたい。Windows の理解も必要。LLM より Jev の良さを」。
次の 5 つを実装し、テスト 328 件がすべて通ったうえで再起動済み。**どれも実地では確かめていない。**

1. **ダイアログへの返事**：前面がダイアログのとき、「はい」「いいよ」「保存しない」「閉じて」「次へ」「置き換えて」でボタンを直接押す（`winctx.dialog_target`・`_answer_dialog`）。
2. **Windows の状況を Jev に渡す**：`facts.windows` に、window_kind・dialog_buttons・focused・explorer_folder・explorer_selected を入れる。
3. **候補が割れたら番号で選ばせる**：Jev の対象の確率が割れているとき（例：上書き保存 27% と 保存して閉じる 23%）、はい/いいえではなく、画面の候補の上に番号を出して「どれですか？」と聞く（`_offer_choices`・hints mode "choices"）。
   - 番号を答えると実行し、その答えを学習する。
   - LLM とエージェントより前に行う。
   - 架空の画面で、Jev の確率から番号が出ることは確認済み。
4. **エクスプローラーの操作**：
   - 「一つ上のフォルダ」「新しい順に並べて」「3番目のファイルを開いて」
   - 「これをデスクトップにコピー」「ダウンロードに移動して」（移動は確認を挟む）
   - 「パスをコピー」「ここでターミナル開いて」
   - 前面がエクスプローラーのときだけ動く。
5. **マウスの連続移動**：「ゆっくり右に」「右にずっと」で動き続ける。
   - 「そこ」「ストップ」で止まり、「そこでクリック」で止めてクリックする。
   - 動いている間の「速く」「ゆっくり」で速さが変わる。

## 次のロードマップ（2026-09-22 夜に設計。ユーザーと相談して順に実装する）

### シェルレーン（ターミナル操作）は却下（ユーザーの判断、2026-09-22）
「ターミナル操作はやめときましょう」。PC 操作（Win32・UIA・Shell COM・Python 標準ライブラリ）の範囲で
同等の便利さを目指す。以下の旧設計は参考として残すが実装しない。

### 実装済み（2026-09-22 追加 8 回目。テスト 555 件合格）
1. **配置のひな形**（config の `layouts`）: 「開発の配置」「資料の配置」「配置一覧」。
   place は left_half/right_half/upper_half/lower_half/full/top_left〜bottom_right/center/maximize。
   開いていないアプリは起動してから並べる。
2. **選択ファイルの zip・展開**（エクスプローラーが前面のとき）: 「これをzipに」「これを圧縮して」
   「zipを展開して」「解凍して」。Python の zipfile（ターミナルなし）。zip は選んだものの隣に作り、
   同名は -2 で避ける。展開先は「zip名のフォルダ」（重複時 -2）。
3. **チェックサム**: 「チェックサム見せて」「〜のハッシュ教えて」→ 選択中ファイルの SHA256 をカード表示
   ＋クリップボードへ（hashlib）。
4. **画面をクリップボードへ**: 「画面を撮ってコピー」「スクショをクリップボードに」→ Qt のシグナルで
   GUI スレッドから grabWindow → クリップボード（「貼り付け」で貼れる）。
5. **アプリ別の音量**（音量ミキサーのセッション単位）: 「Chromeの音量だけ30」「ブラウザの音だけ下げて」
   「Discordの音消して」「VLCをミュート」。「パソコンの音量30」は全体の音量にフォールバック。
   アプリ名の一致はプロセス名＋`APP_VOLUME_ALIASES`（クローム/ブラウザ/エッジ…）。
6. **重いプロセス**: 「メモリ食ってるの見せて」「CPU使ってるの教えて」「重いの見つけて」→ 上位 5 を表示（psutil）。
   止める操作はまだ（誤爆防止のため未実装）。
7. **left_half/right_half/full を直接配置に変更**: Win+矢印キーではなく作業領域の割合で直接置く
   （`SNAP_RECTS` に統合。より確実）。

### 残る候補（シェルなしの PC 操作で）
- 後で読む（ブラウザの URL を UIA で読んで Obsidian へ。ブラウザごとの住所バー読み取りが必要）
- 会議ミュート（Teams/Zoom のプロファイルコマンド）
- 重いプロセスを番号で止める（確認あり。ヒント番号と組み合わせる）
- 「このフォルダのサイズ」・ダウンロードの検証続き

## 配布に向けて：設定 UI の設計（2026-09-22 深夜に整理。実装は未着手）

**価値の整理**（ユーザーの言葉「いままではそれぞれ特化したアプリが行っていた。それを自然言語でできる」）:
クロック（アラーム）、音量ミキサー、エクスプローラー、窓配置ユーティリティ、圧縮ソフト、スクショツール、
マクロツール（ルーチン・記録）——それぞれ専用アプリやショットキー暗記が必要だったものを、
「〇〇して」という一つの文法で統一し、しかも使うほど言い方を覚える。これが voicectl の主張。

**現状の設定は config.yaml 前提で、開発者以外に配布できない**。配布時は設定 UI が必須（ユーザーの判断）。

### 設定 UI の設計方針
- PySide6 の設定窓（トレイメニュー「設定」から開く。既存の Qt に乗せる・新依存なし）
- 保存先は config.yaml のまま（CLI 編集と共存。UI は読み書きするだけ。保存後に再起動を促す）
- 声では設定を変えさせない（一覧性と誤爆防止のために UI の領分）

### タブ構成（案）
1. **マイク・キー**: デバイス選択（一覧＋テスト録音とレベルメーター）、話しかけキー・緊急停止キー（キーキャプチャ）
2. **判定**: 実行/確認のしきい値スライダー、Jev・文章アシスタント・読み上げのオンオフ
3. **窓と配置**: layouts エディタ（app × place のテーブル、place はプルダウン）、マウス移動量・スクロール量
4. **覚えたもの**: 学習した言い方の一覧（成功回数・確認なし昇格の状態・削除）、ルーチン一覧（削除・試し再生）
5. **連携**: Jev の API キー（マスク入力→setx）、アラームのクロックアプリ連携、ウェイクワード（現状無効）
6. **初回ウィザード**（配布時）: マイク確認 → キー選択 → API キー → GPU/STT の確認 → テスト発話

### 配布時の checklist（UI 以外）
- 依存の同梱（venv か PyInstaller。faster-whisper＋CUDA/cuDNN が重い。GPU のない PC への CPU フォールバック）
- Jev API キーの初回入力導線（環境変数登録を UI から行う）
- 自動起動（スタートアップ登録のオンオフ）
- プライバシーの明示（音声がどこへ送られるか: STT はローカル、判定は Jev、文章は DeepSeek）
- 設定のバージョン移行（config.yaml に version フィールド）
- ログの見せ方（「ログフォルダを開く」は既存。不具合報告の導線）

## 2026-09-23 追加（11 回目）：実験ループで「残ったこと」を潰した

「残ったことを整理して実装。実験も自分でループして改善」。テスト 582 件合格。

- **タスクバーの言語依存を解消**: 除外名を英日両対応（`_TASKBAR_SKIP`。大小文字を無視して比較）
- **`taskbar_click` に dry_run 引数**: 押さずに「名前@x,y」を返す。実験と座標確認に使う

### 実験の結果（2026-09-23 未明・私が実行したもの）
| 実験 | 結果 |
|---|---|
| E1 タスクバー列挙（3 周回） | 41 ボタン・毎回同一。スタート/タスクビューは除外できている |
| E2 座標の dry-run（3 周回） | 毎回同一座標（例: WPS@1505,2112）。決定的 |
| E3 実クリック＋フォーカス復元 | 5 番目「付箋」が前面に出て、元の窓（ZCode）への復元も成功 |
| E4/E4b ブラウザ URL | 前面がブラウザでなければ None（正）。裏の Brave 窓から https://github.com を取得 |
| E5 拡大鏡の起動/終了 | magnify.exe の起動を確認、kill で終了（直後の再確認は 1 秒では早すぎた） |
| E6 プロセス停止 | 自前の試験プロセスを kill_process で停止（生存確認 False） |
| E7 会議プロファイル | teams/zoom とも 5 コマンド読み込み（ms-teams/teams/zoom の 3 表記で一致） |
| E8 ウィザード構築 | オフスクリーンで 6 ページ構築 OK |
| E9 pip-audit | **既知の脆弱性 0 件**（オフライン監査の 1 件の該当表示は裏取り済みで問題なし） |

### これで残るもの
- Teams/Zoom の実地（アプリを入れての動作確認）だけが実地未確認。拡大鏡・タスクバー・URL 読みは実験済み
- 監査の「coverage 不完全」注記は構造的なもの（動的派手の呼び出し）。継続的な全監査ではなく、
  大きな変更のつど Mimosa deep を走らせる運用で対応
- pip-audit は監査用ツールとして手動実行（requirements.txt には入れない）

## 2026-09-22 追加（10 回目）：DESIGN.md のフェーズ A〜C を一括実装

「Cまで一気に行ってください」。テスト 582 件合格。**ほとんど実地未確認**（各完了の定義は DESIGN.md）。

### フェーズ A（配布の完成）
- **A1 初回ウィザード**: settings_ui.py に SetupWizard（QWizard）。ようこそ→マイクのテスト→キー→
  API キー（setx）→GPU の確認（pynvml の名前表示のみ。STT は読み込まない）→完了（保存して再起動）。
  トレイ「初回セットアップ」＋**API キーが無い状態で起動すると自動で開く**（`has_api_key()` は
  環境変数とレジスタリの両方を見る。値は表示しない）
- **A2 設定の移行**: config.py `migrate()`。version 未満なら新セクションの既定値を埋める
  （ユーザーの値は上書きしない）。load() が通す。`version: 1`
- **A3 CPU フォールバック**: stt.py `pick_device()`。`stt.device: auto|gpu|cpu`（既定 auto）。
  指定 GPU が無ければ自動で CPU（int8）に落ちる
- **A4 配布形態の確定**: **同梱フォルダ方式**（PyInstaller 不採用）。`run.bat` が初回に venv 作成と
  pip install を自動実行するようになった。手順は `DIST.md`
- **A5 全体監査**: deep スキャンは 1 度ワーカーが落ちたため再実行中。結果は別途

### フェーズ B（精度）
- **B2 ありません応答**: 聞き返しのとき、名前で指したものが画面の候補（類似 0.6 未満）なら
  「「〇〇」は画面にありません」と表示（`_screen_has`）
- **B3 ターミナル文脈**: Windows Terminal では「コピー」「貼り付け」が Ctrl+Shift+C/V に変わる
  （`copy_term`/`paste_term` キー。keyword は facts.window_kind を見る。即実行の前段では
  ターミナルの copy/paste を必ず止めて通常経路へ）。cmd.exe 等は対象外
- **B4 タスクバー**: 「タスクバーの3番目」「タスクバーのBrave」。winctx.taskbar_click が
  Shell_TrayWnd のボタン（スタート/タスクビューを除く）を読んで座標クリック
- **B5 予測バー**: 話している最中の部分認識で確定できる操作を「→ 〜」と状態表示に出す
  （`transcript.predict: true`。fast_path のものだけ・コストほぼゼロ）
- **B1 実地ループ**: 声での試用はユーザーと一緒に（ログ分析の型は既存）

### フェーズ C（統合の深掘り）
- **C1 後で読む**: 「後で読む」→ 前面ブラウザの URL を UIA の ValuePattern で読み（アドレスバーの
  Edit。値にスキームがなければ https:// を補う）、title 付きで state/read_later.txt に追記。
  ブラウザが無いときは直前の last_url。「後で読むリスト」で一覧
- **C2 会議プロファイル**: profiles/teams.yaml・zoom.yaml（ミュート/カメラ/手挙げ等のショートカット）。
  Teams・Zoom をインストールしていないので動作未確認
- **C3 プロセス停止**: 「1番を止めて」→ 直前の proc_top の一覧から確認（はい/いいえ）→ kill。
  `kill_process` は名前が変わっていたら（PID 再利用）中止。`_pending_proc_kill`
- **C4 拡大鏡**: 「画面を拡大」「画面を縮小」（Win+Plus/Minus。拡大鏡が無ければ開く）

### 既知の残り
- タスクバーのボタン読み取りは Win11 の言語に依存（スタート/タスクビューの除外名が日本語固定）
- Teams/Zoom プロファイル・拡大鏡・後で読むのブラウザ別動作（Chrome/Edge 系は確認済み、他は未確認）は実地で
- A5 の監査結果の処理

## 2026-09-22 追加（9 回目）：設定ウィンドウ ＋ 配布準備の開始

「必要そうなタブは組み込んでください。配布物とする準備を始めてください。ただしまだまだ今後も開発を続けます」。
テスト 562 件合格（settings 6 件追加）。voicectl 再起動済み（23:31・エラーなし）。**設定窓の実地は未確認。**

- **設定ウィンドウ**（`voicectl/settings_ui.py`、トレイ「設定」から開く。config.yaml を読み書きし、
  CLI 編集と共存）:
  - マイク・キー: 話しかけキー・緊急停止キー（KEY_VK の一覧）、マイク選択＋「テスト」録音（RMS 表示）、
    途切れ実行のオンオフ
  - 判定: しきい値スライダー（execute/confirm）、LLM 言い直し・アシスタント・読み上げ・クロック連携・
    ウェイクワードのオンオフ
  - 窓と配置: layouts の表エディタ（配置名 × アプリ × 場所プルダウン。未入力行は捨て、不明な場所は full に直す）、
    マウス移動量・スクロール量、files.extra_roots
  - 覚えたもの: 学習の一覧＋削除（`learner.forget` で即時反映）、ルーチン一覧＋削除（即時反映）
  - 連携: TYPESAFE_API_KEY をマスク入力 → setx で環境変数へ（キーはファイル・ログに書かない）
  - 「保存」は config.yaml への書き戻し（`Config.save()`、テンポラリ + os.replace の置き換え方式）。
    「保存して再起動」は run.bat を起動して qapp.quit()（subprocess での引数組み立ては避けた）
- **テスト**: ロジック部分（layout_to_rows/group_layout_rows/parse_roots/apply_changes/Config.save の往復）＋
  オフスクリーンで SettingsDialog を組み立てて `_collect()` まで通すテスト（tests/test_settings.py）。
- **配布準備**:
  - `requirements.txt`（この環境の pip freeze。CUDA 付き。GPU のない PC は今後の課題）
  - config.yaml に `version: 1`（今後の設定移行チェック用）
  - README に「設定ウィンドウ」「配布について」の章（前提・プライバシー・残る作業）
- **未着手（配布に向けての残り）**: 初回ウィザード、インストーラー化（PyInstaller か同梱フォルダか）、
  CPU フォールバック、設定のバージョン移行処理。settings_ui の `_dialog` はモジュール変数（設定窓は 1 枚だけ）。

## 2026-09-22 追加（7 回目）：アラームを Windows 標準のクロックアプリで設定する

「アラームが標準アプリ使っていない」。実機で動作確認済み（**22:53 のテストアラームがちゃんと鳴った**、
とユーザー確認）。テスト 529 件合格。

- 「7時に起こして」「20時15分にアラーム設定して」→ `intents.set_clock_alarm(target, "voicectl")` が
  標準のクロックアプリ（ms-clock:）に設定する。キー入力なし（UIA の Invoke のみ）。
  流れ: ms-clock: を開く → ナビ「アラーム」タブ → AddAlarmButton → HourPicker/MinutePicker を
  上下ボタンで合わせる → 名前「voicectl」を ValuePattern で設定 → PrimaryButton 保存 →
  一覧に同時刻の AlarmTime が出るまで（最大 8 秒）確認。
- クロックアプリを開けない・UI が変わった・時間内に終わらないときは従来の内部タイマーで代用
  （`alarm.use_clock_app: false` で常に内部タイマー）。
- 実装上の工夫: 時・分のピッカーは Value/Range パターン非対応のため上下ボタンを押す。
  どちらが「+」かは実行時に 1 回押して確かめ（校準）、最短の向きで回し、最後に値を読み戻して
  ずれていれば押し直す。クリック中にツリーが組み替わってピッカーが一時的に消えるため、
  再検索つき（`_set_picker`）。保存後の一覧確認もリトライつき（組替え直後は見えないため。
  ここを短絡判定すると内部タイマーが二重に掛かる）。
- 12 時間表示の PC では PeriodPicker の午前/午後を選ぶ実装だが**未検証**（この PC は 24 時間表示）。
- タイマー（「5分後に知らせて」）は従来どおり内部タイマーのまま。
- 残務: 検証で作った「voicectl テスト」（22:53・1 回のみ）は鳴って自動でオフになっているはず。
  次にクロックアプリを開いたときに消すか、ユーザーに消してもらう。

## 2026-09-22 追加（6 回目）：ログ分析による精度改善ループ ＋ 回帰マトリクス（tests/test_accuracy.py）

「精度がまだまだひよっこなので、あなた自身でループ改善を繰り返してください。テストも増やして」。
テスト 527 件合格（この日 最大 379 → 527。マトリクスで +148）。

### やったこと（改善ループ 3 周）
1. **実ログ分析**（`logs/decisions-20260922.jsonl` の 188 判定）: エンジン別内訳・低確信度 45 件を精査。
   しきい値の設計は健全と確認（低確信度の keyword も `_act` で確認待ちに落ちるので、危険な誤実行はなかった。
   朝の「保存ボタンを押して」0.05 などは確認待ち）。
2. **発見して直したもの**:
   - 「Cドライブ開いて」→ ドライブのルートを開く（`tasks` の `open_drive`、A〜Z）
   - 「会議メモのファイル出して」→ ファイル検索（`_p_file` に「〜のファイル(を)開いて/出して」の形を追加。
     「ダウンロードの」「さっきの」などの特殊フォルダ語は除外）
   - 「メモ帳に自己紹介書いて」→ AI の文章作成（`_WRITE_KINDS` に「自己紹介」）
   - 「選択して開いて」→ エクスプローラーで選択中の項目を開く（`winctx.explorer_open_selected`、
     エクスプローラーが前面のときだけ）
   - 「スクリーンショットして」「〜してみて」「画面全体スクリーンショットして」→ キー即実行
   - 「オンリョウを30にして」（カタカナの音量）
3. **マトリクスは実ログの発話と README の言い方から作り、振り分け（intent／task／fast／keyword／Jev）を
   `route()` で一括検証**する。controller の guard（`_FAST_VERB`・`_names_on_screen`）も模倣しているので、
   「保存を押して」が画面に保存ボタンのあるとき Jev に回ることまで見える。
4. **朝の誤判定の多くは日中の改善で既に直っていることを確認**（Google マップ・X・アラーム 20 時 15 分・
   ボリューム三割など。当時は Jev が 0.00〜0.5 で迷っていた）。

### 実ログに残った未解決（次のループ候補）
- 「YouTubeを開いてリュウジさんの動画までたどり着いて」→ 分け実行の判定で task が捨てられ Jev が迷子。
  分け実行のときも先頭が目的ひな形ならそれを生かすとよい。
- 「ピオソルバー開いて」が Jev 0.00（「none」）になることがある → app_readings やプロファイル用語の
  hotwords 強化、またはカタログ一致時の keyword 優先の拡大。
- 「違う」「違います」が確認待ち以外で不明命令になる → 直前の確認・直前の候補に対する否定として扱う。
- 会話っぽい発話（「わからない場合は教えてください」）は unknown で正しい。`is_pc_command` で静かに捨てる案。

## 2026-09-22 追加（5 回目）：初動の高速化 ＋ Windows 操作の追加 ＋ 読み上げの無効化

「もっと Windows 操作性を高めたい。すべてを撃せなくても初動は必ずすぐ始めたい」「読み上げる機能いりません」。
テスト 379 件合格、スモーク合格。**キー・ウィンドウ操作を伴うものは実地未確認。**

### 初動の高速化（2 つ）
1. **確定の短い命令は画面を見ずに即実行**（`controller._handle_text` の `context.collect()` の前）:
   「右」「クリックして」「左半分にして」「コピーして」「最小化」「もう一回」など、キーワード照合で
   確信度 0.95 以上・画面の候補を必要としない命令は、UIA・OCR の収集（0.1〜0.8 秒）を待たずに実行する。
   画面の名前で押す言い方（key_combo ＋「押して/開いて/見せて」等、`_FAST_VERB`）は対象外（従来どおり）。
   エンジン表示は `keyword(即実行)`。
2. **話している最中の認識を確定に再利用**（`transcript.partial_as_final`、既定 true）:
   文字起こしバー用の途中認識が（1.5 秒以内で、その後の追加音声が 0.5 秒以内なら）確定としてそのまま使われ、
   離したときの認識（約 0.35 秒）を省く。条件は `Controller._can_reuse_partial`。
   注意: 途中認識は前面アプリの用語ヒント（extra_vocab）なしで認識しているので、固有名詞が揺れる可能性あり。

### Windows 操作の追加
- **上半分・下半分・中央・四分の 1 への配置**（Win+矢印では出せない形）: 「上半分に」「下半分にして」
  「中央に置いて」「左上に置いて」「右上の四分の一」。`schema.SNAP_RECTS` の割合で `winutil.place_rect` が
  直接置く。マウス移動の「左上」と区別するため、四分の 1 は「置いて」などが必須（「左上」だけはマウス）。
  Jev の snap の選択肢にも入った。
- **上下に 2 分割**: 「Chromeを上Obsidianを下に並べて」「上にChrome下にメモ帳」「ChromeとObsidianを上下に並べて」
  「縦に並べて」（`intents.arrange(vertical=True)`）。
- **読み上げ（音声返答）を無効化**: ユーザーの依頼。`voice_reply.enabled: false`。答えはカードに文字で出るだけ。

## 2026-09-22 追加（4 回目）：ウェイクワード（ハンズフリー）— ユーザーの依頼で無効化済み

「待機中は声に反応させて、自然に話しかけるだけで動くようにしたい」の依頼で実装したが、
**試してもらったところ F5 の反応が悪化したため、ユーザーの依頼で無効化した**（config `wake_word.enabled: false`）。

原因の推定（未検証）：待機中に周囲の音で確認認識（whisper）が頻発し、STT の排他ロックを
取り合って F5 の認識が待たされた可能性。再挑戦するなら:
- `wake_word.level` を上げる（環境音では始まらない高さに）
- `wake_word.min_interval_sec` を長くする（例 5〜10 秒）
- 確認認識を小さいモデルにする／F5 押下中は確認を即中断する、などの変更

コードは残してある（`wake.py`・`Recorder.generation`・`wake_session_start`）。トレイの
「ウェイクワードで開始（ハンズフリー）」か config でいつでもオンに戻せる。テスト 363 件合格のまま。

- 待機中、マイクの音量を監視し、話し声が終わったところで短く認識して `wake.py` の `match()` で
  ウェイクワード（既定「コンピューター」、config の `wake_word.words` で変更可）を探す。
- 呼びかけだけ → `wake_session_start()` で常時聞き取りセッション開始（話すたびに実行）。
  `wake_word.session_idle_sec`（既定 12 秒）無音で自動待機に戻る。F5 タップでも終了。
- 「コンピューター、音量30」のように一言に続けた → 続きの文をそのまま命令として実行。
- F5 との競合は録音の世代番号で防ぐ（`Recorder.generation` / `stop_if_generation`）。
  ウェイク確認中に F5 が押されたら確認は無言で中止し、F5 側を優先。
- 聞き取り中・書き取り中・一時停止中・読み上げ中はウェイクを聞かない。確認の最少間隔 2 秒。
- トレイに「ウェイクワードで開始（ハンズフリー）」のオン・オフあり。config は `wake_word:`。
- 既知の注意: 待機中に周囲で話し声がするたびに短い認識（GPU 約 0.3 秒）が走る。
  騒がしい環境では `wake_word.level` を上げるか `enabled: false`。

## 2026-09-22 追加（3 回目）：もっと操作・もっと便利（8 つの新しい操作）

「もっと操作させて便利なことを追求したい」の依頼で、日常的に効く操作を追加。テスト 361 件合格、
スモークテスト合格、`latest_download()` は実機で読み取り確認済み。**キー入力を伴うものは実地未確認。**

1. **次のウィンドウ／前のウィンドウ**: Alt+Esc でウィンドウを切り替える（`next_window` / `prev_window`）。
2. **画面を撮って**: Win+PrtScn で全画面を画像に保存（`screenshot_save`）。「切り取って」は従来どおり範囲選択。
3. **音楽・動画の自然な言い方**: 「音楽止めて」「音楽流して」「動画を止めて」→ 再生/一時停止キー。
4. **ウィンドウを常に手前に**: 「常に手前にして」「常に手前を解除」（`winutil.set_topmost`）。
5. **行の編集**: 「この行を選んで」（Home → Shift+End）、「行を消して」（＋ Delete。エディタでは元に戻せる）。
6. **これで検索して**: 選択中の文字を Ctrl+C で取って Google 検索（クリップボードは元に戻す）。
7. **さっきダウンロードしたのを開いて**: ダウンロードフォルダの最新ファイル（.crdownload 等は除く）を開く。
   OneDrive リダイレクトにも対応（SHGetKnownFolderPath）。「ダウンロードを開いて」（フォルダ）と区別する。
8. **音声メモ**: 「メモっておいて〇〇」（前置き・後置き両対応）→ `state/memos.txt` に追記。
   「メモ見せて」「メモ読んで」（読み上げ）「メモ全部消して」（はい／いいえの確認あり）。

## 2026-09-22 追加（2 回目）：Windows の状況の先読み

`winctx.collect`（窓の種類・ダイアログ・フォーカス・エクスプローラーの状態。0.1〜0.6 秒）を、
F5 を押した瞬間の `context.begin()` で UIA・OCR と並列に先読みするようにした（`context.py` の
`win_context()`）。判定時（`controller._decide`）は先読み結果を使い、前面の窓が変わっている・
10 秒以上古い・時間内に終わらないときだけ従来どおりその場で取る。
テスト 333 件合格。読み取りのみの実機確認（入力なし）で先読みが WinContext を返すことも確認済み。
**実地（声での体感速度）は未確認。**

### 未確認・次にやること

- **実地確認**（3 回目の 8 操作 + 2 回目の先読み + 最初の 5 機能。4 回目のウェイクワードは無効化済み）。確認する言い方（抜粋）:
  - 新操作: 「次のウィンドウ」「常に手前にして」→「常に手前を解除」「画面を撮って」（ピクチャ直下のスクリーンショットに入る）
    「音楽止めて」「この行を選んで」「行を消して」「これで検索して」（何か文字を選んでから）
    「さっきダウンロードしたのを開いて」「メモっておいてテスト」→「メモ見せて」→「メモ全部消して」→「はい」
  - ダイアログ: 何かの保存確認を出して「保存しない」「キャンセル」
  - 番号選択: 確率が割れる場面で番号が出るか、「5番」で実行できるか
  - エクスプローラー: 「新しい順に並べて」「3番目のファイルを開いて」「これをデスクトップにコピー」「パスをコピー」
  - 連続移動: 「ゆっくり右に」→「速く」→「そこでクリック」
  - 先読みの体感: Jev 経由の命令が以前より速く感じるか（decisions ログの `ms` で数値でも見られる）
  - ダイアログの実地テストは、テスト用メッセージボックスを出す案を一度断られた。やるなら先に了承を得る。
- **次の改善候補**（Jev を活かす方向）:
  - 話している最中に、途中の文字起こしから「→ 保存をクリック」のように、実行する操作の予想をバーに出す。
  - タスクバー・通知領域・スタートメニューの理解（「タスクバーの3番目」「Wi-Fi のアイコン」）。
  - 存在しないメニューを頼まれたとき（PioViewer の「新しいツリー」など）に「ありません」と返す。
  - 状況に応じて同じ言葉の意味を切り替える範囲を広げる（ブラウザ・エディタ・メディア）。
  - 音声メモの発展: 「メモの〇〇のとこだけ消して」「メモをメールで送って」など。
- **既知の問題**：
  - PioViewer で「レンジを保存して」が Range IP（レンジの表示）と誤判定される。
  - DeepSeek の下書きは、日付などを取り違えることがある。
  - テスト用ファイル out/voicectl_test.txt（メモ帳で開いている）には、保存していない改行が残っている。

## 確認のしかた

- **テスト**：`.venv\Scripts\python.exe -m pytest -q`（pyenv の python には pykakasi がないので使わない）
- **振り分けの確認**（操作はしない。Jev と DeepSeek の API は呼ぶ）：`.venv\Scripts\python.exe tests\route_check.py [言い方のファイル]`。PioViewer を想定するときは `ROUTE_APP=pio` を付ける。
- **UI の見本**：`.venv\Scripts\python.exe tests\overlay_preview.py` で out\overlay_preview*.png を書き出す。
- **配置の確認**：`tests\overlay_check.py`
- **再起動**：voicectl を実行している pythonw を止め、run.bat を起動する。ログは logs\voicectl.log と logs\decisions-YYYYMMDD.jsonl。
- **文字で命令を送る**：Python の urllib から `http://127.0.0.1:8766/say` に UTF-8 で POST する。Git Bash の curl では日本語が化ける。
